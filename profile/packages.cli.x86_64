# Castle OS — headless CLI mode extras.
# Router / NAS / server personality. Samba + Docker pulled forward from
# Phase 2 (2026-09-12): the live USB is a real server from first boot.

nftables          # firewall (router role groundwork)
wireguard-tools   # VPN tunnels (Castle drawbridge)
dnsmasq           # DNS/DHCP for the LAN side
samba             # Windows/SMB file shares (NAS role)
docker            # containers (Pi-hole and friends run here, not native)
smartmontools     # disk health
usbutils          # lsusb for hardware poking
pciutils          # lspci for hardware poking
